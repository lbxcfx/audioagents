python-dotenv==1.2.2
httpx==0.28.1
openai==2.43.0
# Keep the LiveKit SDK family aligned. livekit-agents pins the native SDK exactly.
livekit==1.1.13
livekit-api==1.2.0
livekit-agents==1.6.6
livekit-plugins-openai==1.6.6
livekit-plugins-silero==1.6.6
# Text-based, multilingual end-of-turn detection. Keep this aligned with Agents.
livekit-plugins-turn-detector==1.6.6
# Keep the deprecated plugin on its last compatible Transformers 4.x line.
transformers==4.57.6
fastapi==0.138.0
uvicorn==0.49.0
cryptography==49.0.0
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
PyJWT[crypto]==2.13.0
# Explicit security floor for the image codec pulled in by LiveKit local inference.
pillow==12.3.0
