# 个人电话助理研发计划

## 1. 项目目标

将当前 AudioAgent 建设为面向真实手机用户的 AI 电话助理服务。用户开通服务后，将本人手机号的来电通过运营商呼叫转移到平台号码；平台自动接听并完成语音对话，通话结束后生成结构化报告，通过微信通知用户。

首版业务闭环：

```text
用户开通服务并绑定微信
  → 平台分配专属 DID
  → 用户设置无人接听/遇忙呼叫转移
  → 外部来电被转入平台 SIP Trunk
  → AudioAgent 自动接听并对话
  → 保存号码、时间、转写、录音及呼叫状态
  → 异步生成摘要、待办和标签
  → 微信发送摘要通知
  → 用户进入受保护的详情页查看和处理
```

### 1.1 首版成功标准

- 开通用户能绑定一个真实手机号和微信身份，并获得一个专属平台 DID。
- 无人接听或遇忙时，真实来电能稳定转入 AudioAgent。
- 系统能正确识别服务归属用户和真实主叫号码。
- AI 能自动接听、完成双向对话并正确结束通话。
- 通话结束后 60 秒内生成报告，报告包含号码、时间、时长、摘要、逐字稿和标签。
- 微信通知发送成功率不低于 99%，失败任务可自动重试和人工补发。
- 多用户之间的数据、线路、通知和权限严格隔离。
- 未开通、欠费、停用、超并发或不满足策略的呼叫不会被错误接听。

### 1.2 首版不包含

- 直接控制 Android 或 iPhone 自动接听系统电话。
- 号码携转、实体 SIM 托管和自建运营商核心网。
- AI 主动外呼。
- 使用非官方接口向个人微信号发送消息。
- 自动执行转账、下单、改签等高风险操作。

## 2. 产品与运营商方案

### 2.1 默认接听模式

首版支持以下策略：

| 策略 | 行为 | 建议用途 |
|---|---|---|
| 无人接听转移 | 手机响铃一定时间后转给 AI | 默认策略 |
| 遇忙转移 | 用户正在通话时转给 AI | 默认开启 |
| 无条件转移 | 所有来电直接转给 AI | 夜间、休假模式 |
| 停止服务 | 取消呼叫转移或禁用平台路由 | 用户主动关闭 |

呼叫转移的开通和取消方式由运营商决定。产品端展示对应运营商的设置说明和状态自检，但不得宣称平台能够直接替用户修改运营商设置，除非后续取得正式运营商 API。

### 2.2 DID 分配原则

MVP 使用“一名用户对应一个专属 DID”。不能仅凭主叫号码识别服务归属，因为同一个来电者可能拨打多个用户。

必须向 SIP 线路供应商确认：

- 支持呼入 DID 和 SIP Trunk。
- 呼叫转移场景下保留真实主叫号码。
- 能通过 Request-URI、`To`、`Diversion` 或 `History-Info` 标识平台 DID/原始被叫号码。
- 提供呼叫唯一标识、接通和挂断原因。
- 明确号码实名、并发数、CPS/TPS、地域、资费和故障 SLA。
- 明确是否支持 SIP TLS、SRTP、IP 白名单及 Webhook 签名。

如果供应商不能稳定提供原始被叫号码，仍可用专属 DID 找到用户；不得在多个用户之间共享一个无法区分归属的 DID。

### 2.3 运营商 PoC 门禁

在业务代码全面开发前，用一个真实手机号和一个真实 DID 完成以下验证：

1. 分别验证无条件、无人接听和遇忙转移。
2. 采集完整 SIP INVITE，确认真实主叫号码和 DID 所在字段。
3. 验证接通、主叫挂断、AI 挂断、拒接、超时和重复 Webhook。
4. 验证 DTMF、中文语音质量、双向音频和 30 分钟通话。
5. 验证线路中断后平台不会重复接听或产生无法结束的占用。
6. 形成《运营商字段映射表》和《线路验收报告》。

未通过主叫号码、用户归属和挂断状态三项验证，不进入生产研发阶段。

## 3. 总体技术架构

```text
手机运营商呼叫转移
        │
        ▼
平台 DID / SIP Trunk
        │
        ▼
LiveKit SIP ── Dispatch Rule ── LiveKit Room
                                      │
                                      ▼
                              Phone Agent
                        ASR → Dialogue/LLM → TTS
                              │          │
                              │          └─ 标签候选事件
                              ├─ 双方逐轮转写
                              ├─ 呼叫状态/录音
                              ▼
                         Control Plane API
                              │
                    PostgreSQL + 对象存储
                              │
                              ▼
                       Outbox / 消息队列
                              │
                              ▼
                       Post-call Worker
                    摘要 → 标签 → 待办 → 报告
                              │
                              ▼
                       Notification Worker
                              │
                              ▼
               公众号/小程序或企业微信官方接口
```

### 3.1 复用当前能力

- `qwen-telephony/agent/phone_agent.py`：LiveKit Agent、呼入准入、通话状态、录音。
- `qwen-telephony/server/cloud_parity/telephony_api.py`：电话控制面 API。
- `qwen-telephony/server/dialogue.py`：场景话术和规则标签。
- `qwen-telephony/server/cloud_parity/store.py`：商用数据模型和存储迁移。
- LiveKit Egress：录音写入对象存储。

### 3.2 新增模块

```text
qwen-telephony/server/cloud_parity/
  inbound_routes.py          # 用户、DID 和接听策略
  transcripts_api.py         # 转写批量写入和查询
  call_reports.py            # 报告查询、修改标签、处理状态
  wechat_bindings.py         # 微信绑定与回调
  notification_api.py        # 通知查询和人工补发

qwen-telephony/server/services/
  post_call_service.py       # 通话后编排
  summary_service.py         # 结构化摘要
  tagging_service.py         # 规则与模型标签
  privacy_service.py         # 脱敏和展示策略
  wechat_provider.py         # 微信渠道适配器

qwen-telephony/worker/
  post_call_worker.py
  notification_worker.py
```

Worker 初期可以使用 PostgreSQL Outbox 轮询，减少新基础设施；达到较大规模后再迁移至 Redis Streams、RabbitMQ 或云消息队列。

## 4. 核心业务流程

### 4.1 用户开通

1. 用户登录 H5、小程序或管理后台。
2. 完成手机号短信验证。
3. 阅读并确认服务、隐私、录音和转写条款。
4. 绑定微信通知身份。
5. 创建用户专属 Agent 配置：称呼、欢迎语、可回答范围、转人工策略、禁答内容。
6. 从可用号码池分配专属 DID。
7. 展示用户运营商对应的呼叫转移指引。
8. 用户使用另一部手机拨打测试电话。
9. 平台校验 DID 路由、主叫号码、AI 接听和微信通知。
10. 测试成功后把服务状态改为 `active`。

开通状态机：

```text
draft → phone_verified → wechat_bound → did_assigned
      → forwarding_pending → testing → active
                                  └──→ test_failed
active → suspended / canceled
```

### 4.2 呼入准入

收到 SIP 呼入后按以下顺序处理：

1. 规范化 `caller_number`、`called_did`、`original_called_number`。
2. 按 DID 查询唯一有效路由，找不到时默认拒绝并告警。
3. 检查租户、用户、套餐、接听时段、并发和服务状态。
4. 检查号码黑名单和用户自定义拒接规则。
5. 使用 SIP Call-ID/运营商 Call-ID 创建幂等呼叫记录。
6. 将 `user_id`、`route_id`、号码字段和接听策略写入加密的 Job metadata。
7. 创建 Insights Session，启动心跳和可选录音。
8. 播放录音/AI 身份告知，再开始业务对话。

准入失败必须有明确状态：`route_not_found`、`service_inactive`、`outside_schedule`、`capacity_exceeded`、`blocked_number` 或 `internal_error`。

### 4.3 通话处理

- 欢迎语应说明 AI 身份；启用录音时先完成录音告知。
- 每个 ASR 最终片段保存为 `caller` 转写。
- 每个最终 TTS 文本保存为 `assistant` 转写。
- 每条记录包含序号、相对时间、文本、置信度和模型信息。
- Agent 只允许访问当前用户授权的知识和工具。
- 用户要求人工、出现投诉/紧急事件、连续无法理解时，按配置转人工或礼貌结束。
- Agent 不得承诺未接入系统的业务结果。

### 4.4 挂断与报告生成

挂断处理必须快速、幂等：

1. 收敛通话终态和挂断方。
2. 停止录音并保存 Egress 状态。
3. 关闭 Insights Session。
4. 在同一个数据库事务中写 `call.completed` Outbox 事件。
5. 由 Post-call Worker 异步生成报告。

Post-call Worker：

1. 锁定事件，检查 `report_version` 是否已生成。
2. 合并并清洗双方转写，过滤空片段和重复片段。
3. 对身份证、银行卡等敏感字段执行报告级脱敏。
4. 先运行确定性规则标签，再调用 LLM 生成结构化摘要和补充标签。
5. 使用 JSON Schema 校验模型输出；失败时重试，最终降级为基础报告。
6. 保存报告、最终标签和待办事项。
7. 写入 `call.report_ready` Outbox 事件。

报告结构：

```json
{
  "summary": "来电人希望将明天下午的预约改到三点。",
  "purpose": "修改预约",
  "result": "已登记，等待用户确认",
  "sentiment": "neutral",
  "urgency": "normal",
  "callback_required": true,
  "tags": ["预约变更", "需要回电"],
  "action_items": ["确认明天下午三点是否可预约"],
  "sensitive_data_detected": false
}
```

### 4.5 标签策略

标签分三层：

1. 系统标签：`未接通`、`通话过短`、`转人工`、`疑似骚扰`、`需回电`。
2. 业务规则标签：复用场景配置中的关键词、意图和节点命中规则。
3. LLM 标签：从项目允许的标签集合中分类，不允许任意创造生产标签。

保存字段包括 `tag_id`、`source`、`confidence`、`rule_version/model_version` 和 `created_at`。规则结论优先级高于 LLM，用户手工修改拥有最高优先级并保留审计记录。

### 4.6 微信通知

仅使用微信官方允许的消息通道：

- 面向个人用户：认证公众号服务通知/模板能力或小程序订阅消息，具体能力以账号资质和当期平台规则为准。
- 面向企业内部人员：企业微信自建应用消息。

不得使用个人微信机器人、模拟登录或逆向协议。

通知只展示必要摘要：

```text
AI 已为您接听一通电话

来电号码：138****5678
来电时间：2026-07-29 14:32
通话时长：02:18
内容摘要：对方希望修改明天下午的预约。
标签：预约变更、需要回电

[查看完整报告]
```

完整号码、逐字稿和录音只在登录后的 H5/小程序详情页展示。录音使用短时签名 URL，不把对象存储永久地址发到微信。

通知规则：

- 以 `call_id + channel + template_version` 作为幂等键。
- 发送失败按 1、5、15、60 分钟退避重试，最多 8 次。
- Token 缓存需支持提前刷新和单飞锁，避免并发刷新。
- 记录请求追踪 ID、返回码和消息 ID，不记录完整号码、正文或密钥。
- 永久失败进入死信状态，并提供后台人工补发。

## 5. 数据模型

生产环境使用 PostgreSQL；号码、微信标识和逐字稿属于敏感数据。

### 5.1 `personal_assistant_users`

| 字段 | 说明 |
|---|---|
| id/project_id | 用户和租户边界 |
| phone_ciphertext | 加密后的真实手机号 |
| phone_hash | 带密钥哈希，用于精确查找 |
| display_name | 用户称呼 |
| status | 开通状态 |
| timezone | 接听计划时区 |
| agent_profile_json | 欢迎语、知识和处理策略 |
| consent_version/consented_at | 同意证据 |

### 5.2 `inbound_routes`

| 字段 | 说明 |
|---|---|
| id/project_id/user_id | 归属关系 |
| did_number_ciphertext/hash | 专属 DID |
| provider/trunk_id | 线路信息 |
| answer_mode | no_answer、busy、always |
| schedule_json | 服务时段 |
| status | provisioning、testing、active、suspended |
| provider_metadata_json | 供应商标识，不保存凭据 |

`did_number_hash + active status` 必须保证唯一。

### 5.3 扩展 `calls`

增加：

- `user_id`、`route_id`、`direction`。
- `caller_number_ciphertext/hash/last4`。
- `called_did_ciphertext/hash`、`original_called_number_ciphertext`。
- `provider_call_id`、`sip_call_id`、`room_name`。
- `started_at`、`answered_at`、`ended_at`、`duration_sec`。
- `hangup_party`、`status`、`failure_code`。
- `recording_status`、`recording_storage_uri`。
- `report_status`、`report_version`、`notification_status`。

### 5.4 新表

- `call_transcripts`：逐轮文本及时间轴。
- `call_reports`：摘要、目的、结果、情绪、紧急程度及待办。
- `call_tags`：规则、模型和人工标签。
- `wechat_bindings`：渠道类型、加密的 OpenID/UserID、授权状态。
- `notification_jobs`：消息状态、次数、下次重试时间和幂等键。
- `outbox_events`：可靠异步事件。
- `consent_records`：条款版本、来源、时间和证据。
- `audit_logs`：查看完整号码、播放录音、改标签和补发消息等操作。

## 6. API 设计

### 6.1 用户和路由

```text
POST   /api/personal-assistant/users
GET    /api/personal-assistant/users/{user_id}
PATCH  /api/personal-assistant/users/{user_id}
POST   /api/personal-assistant/users/{user_id}/verify-phone
POST   /api/personal-assistant/users/{user_id}/assign-did
POST   /api/personal-assistant/routes/{route_id}/test
POST   /api/personal-assistant/routes/{route_id}/activate
POST   /api/personal-assistant/routes/{route_id}/suspend
```

### 6.2 Agent 内部接口

```text
POST /api/platform/projects/{project_id}/telephony/calls/inbound
POST /api/platform/projects/{project_id}/telephony/calls/{call_id}/transcripts:batch
POST /api/platform/projects/{project_id}/telephony/calls/{call_id}/heartbeat
POST /api/platform/projects/{project_id}/telephony/calls/{call_id}/transition
POST /api/platform/projects/{project_id}/telephony/calls/{call_id}/recording
```

转写批量接口要支持 `event_id` 幂等、单调序号校验和最多 100 条批量写入。Agent 本地短时缓存，网络恢复后补交。

### 6.3 报告和微信

```text
GET   /api/personal-assistant/calls
GET   /api/personal-assistant/calls/{call_id}
GET   /api/personal-assistant/calls/{call_id}/report
PUT   /api/personal-assistant/calls/{call_id}/tags
POST  /api/personal-assistant/calls/{call_id}/notifications:retry
POST  /api/personal-assistant/wechat/bind
GET   /api/personal-assistant/wechat/callback
POST  /api/personal-assistant/wechat/events
DELETE /api/personal-assistant/wechat/binding
```

所有用户接口必须从登录身份获得 `user_id/project_id`，不能接受客户端传入的租户身份作为授权依据。

## 7. 安全、隐私与合规

- 在欢迎语中明确 AI 身份；如录音，必须先完成录音告知再启动录音。
- 号码、微信身份、逐字稿、摘要和录音按敏感个人信息管理。
- 数据库字段级加密，号码查询使用独立密钥 HMAC；密钥放入 KMS/Vault。
- API、SIP 信令和媒体根据供应商能力启用 TLS/SRTP。
- 管理员默认只能看到脱敏号码，查看完整号码和录音需要细粒度权限并写审计日志。
- 定义逐字稿、报告、录音、通知日志的保存期限和删除任务。
- 用户取消服务时先停用 DID 路由，再撤销微信绑定，最后按政策异步删除数据。
- 日志禁止出现完整号码、OpenID、逐字稿、录音地址、Bearer Token 和供应商密钥。
- 建立黑名单、骚扰防护、单主叫频控、最长通话时长和紧急停止开关。
- 上线前由法务和线路供应商确认当地的号码实名、呼叫转移、录音告知、转写和数据保存要求。

## 8. 研发阶段与任务拆分

以下以两周一个 Sprint、后端 2 人、Agent/音频 1 人、前端 1 人、测试 1 人估算；商务和运营商等待时间不计入纯研发工期。

### Sprint 0：需求冻结与线路 PoC（1～2 周）

- 确定首发用户、地区、运营商和微信渠道。
- 采购测试 DID/SIP Trunk，配置公网测试环境。
- 完成三类呼叫转移测试和 SIP 字段映射。
- 定义录音告知、欢迎语、默认标签集合和数据保留期。
- 输出威胁模型、数据流图和线路容量约束。

交付物：线路验收报告、字段映射表、接口契约、产品原型、合规清单。

验收：10 次真实呼入全部正确找到用户；主叫号码准确；接通和挂断状态全部收敛。

### Sprint 1：用户、DID 和呼入路由（2 周）

- 建立用户、同意记录、号码池和路由表迁移。
- 实现手机号加密/HMAC、DID 分配与回收。
- 扩展 SIP Dispatch metadata 和呼入准入。
- 实现服务状态、时段、并发、黑名单检查。
- 制作开通流程和呼叫转移操作引导页。
- 增加开通测试电话和激活状态机。

验收：至少 20 个测试用户/DID 可隔离接入；未知 DID 默认拒绝；重复 SIP Call-ID 不产生重复呼叫。

### Sprint 2：转写、录音和通话收敛（2 周）

- 接入 caller/assistant 最终文本事件。
- 实现转写缓冲、批量上传、幂等和断线补交。
- 扩展通话时间、挂断方、失败原因和录音状态。
- 部署 Egress 和受保护的对象存储。
- 实现短时录音访问 URL 和审计。
- 覆盖主动挂断、对方挂断、Agent 崩溃和 Webhook 乱序。

验收：50 通电话转写序号完整且双方角色正确；失败重试不重复；录音与 call_id 一一对应。

### Sprint 3：报告、标签和 Outbox Worker（2 周）

- 创建报告、标签、Outbox 数据模型。
- 实现结构化摘要 Prompt、JSON Schema 和结果校验。
- 实现确定性规则、LLM 标签和人工标签优先级。
- 实现 Worker 租约、重试、死信和恢复。
- 建设报告详情页、通话列表、搜索和处理状态。
- 完成敏感字段脱敏和模型输入边界。

验收：正常通话 95% 在 60 秒内生成有效报告；模型故障时生成基础报告；重复事件不会重复生成。

### Sprint 4：微信绑定与通知（2 周）

- 完成微信账号资质和模板申请。
- 实现 OAuth/订阅或企业微信身份绑定。
- 实现 Access Token 管理和渠道适配器。
- 实现通知任务、退避重试、死信和人工补发。
- 微信消息跳转至登录后的报告详情页。
- 实现退订、解绑和通知偏好。

验收：测试账号通知成功率不低于 99%；重复任务只发一次；链接越权测试全部失败；消息不泄露完整敏感内容。

### Sprint 5：商用加固和小流量灰度（2 周）

- 完成租户隔离、OIDC/RBAC、审计和密钥轮换。
- 完成限流、容量控制、套餐状态和最长通话限制。
- 建立指标、日志、追踪和告警。
- 压测 API、数据库、Agent、SIP 并发和 Worker 积压。
- 演练数据库、LiveKit、Agent、运营商和微信故障。
- 完成备份恢复、数据删除和停服流程。
- 先开放内部用户，再灰度到 10、50、200 名用户。

验收：通过商用上线清单；峰值 1.5 倍压测稳定；故障恢复不重复接听、不丢报告、不重复通知。

## 9. 测试计划

### 9.1 单元测试

- E.164 号码规范化、脱敏、加密和 HMAC。
- DID 路由唯一性和状态机。
- SIP 字段解析及供应商差异适配。
- 转写去重、排序和批量幂等。
- 标签优先级和模型 JSON 校验。
- 通知重试、幂等、Token 刷新。
- 数据保留和权限判定。

### 9.2 集成测试

- SIP INVITE → LiveKit Room → Agent → Control Plane。
- 真实 ASR/LLM/TTS 和模拟 Provider 超时。
- Egress 启停、Webhook 重复及乱序。
- PostgreSQL Outbox 崩溃恢复。
- 微信沙箱/测试账号发送、失效 Token 和限流响应。

### 9.3 端到端场景

- 无人接听转入 AI、遇忙转入 AI、无条件转入 AI。
- 正常咨询、静默、噪声、方言、DTMF、反复打断。
- 来电者先挂、AI 先挂、运营商断线、最长时长强制结束。
- 用户暂停服务、欠费、超时段、超并发和黑名单来电。
- 同一来电 Webhook 重复/乱序，Worker 在每个步骤崩溃重启。
- 微信未绑定、取消订阅、发送超限和链接过期。
- 用户 A 无法访问用户 B 的号码、逐字稿、摘要和录音。

### 9.4 容量指标

压测目标以预计峰值的 1.5 倍执行，并记录：

- SIP 同时振铃、同时通话和每秒新呼叫数。
- Agent 启动延迟、首句延迟、ASR/LLM/TTS P50/P95/P99。
- 通话状态收敛延迟、报告生成延迟、通知发送延迟。
- Worker 队列深度、重试率、死信数。
- PostgreSQL 连接数、慢查询、存储增长。

## 10. 可观测性与告警

核心指标：

- `inbound_calls_total{status,provider}`
- `inbound_route_failures_total{reason}`
- `active_calls`、`call_answer_latency_seconds`
- `transcript_upload_failures_total`
- `recording_failures_total`
- `post_call_latency_seconds`、`post_call_dead_letters_total`
- `wechat_notifications_total{status,code}`
- `notification_queue_age_seconds`

必须告警：

- 五分钟呼入失败率超过阈值。
- 大量 DID 找不到路由。
- 活跃通话长时间无心跳。
- 报告队列最老任务超过五分钟。
- 录音强制模式启动失败。
- 微信连续发送失败或 Token 刷新失败。
- 数据库、LiveKit、SIP Trunk 或对象存储不可用。

值班手册应包含暂停指定路由、全局停止接听、线路切换、补生成报告、补发通知和数据泄露响应步骤。

## 11. 发布与灰度

### 11.1 环境

- Development：MicroSIP 和模拟 Webhook，不保存真实敏感信息。
- Staging：真实测试 DID、微信测试账号、独立对象存储。
- Production：独立线路凭据、KMS、OIDC、PostgreSQL HA、备份和监控。

### 11.2 灰度步骤

1. 团队内部 5～10 个号码运行一周。
2. 邀请 50 名用户，只开放无人接听转移，限制单通 10 分钟。
3. 扩大到 200 名用户，验证账单、并发和客户支持流程。
4. 根据接通率、摘要准确率、通知成功率和投诉率决定继续放量。

每一步必须能够按用户、DID、租户和全局四个层级立即停止接听。

## 12. 项目风险及应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 运营商不保留原始被叫 | 共享 DID 无法找用户 | MVP 坚持专属 DID；更换供应商 |
| 主叫号码被改写或匿名 | 报告号码不准确 | 保存号码来源与可信度，匿名时明确展示 |
| 用户呼叫转移配置错误 | 无法接听或循环转移 | 开通测试、循环检测、显著取消指引 |
| AI 误承诺或越权处理 | 客诉和业务风险 | 工具白名单、固定边界话术、转人工 |
| 转写/摘要包含敏感数据 | 隐私风险 | 最小化、加密、脱敏、权限与保留期 |
| 微信消息规则或资质变化 | 通知不可达 | 渠道适配层、站内通知、短信作为可选降级 |
| Worker 重复执行 | 重复报告/重复通知 | Outbox、唯一键和端到端幂等 |
| 录音或消息成本失控 | 毛利下降 | 套餐限额、时长限制、生命周期清理和成本指标 |
| SIP/LiveKit 单点故障 | 来电无法接听 | 多副本、健康探针、线路溢出和故障演练 |

## 13. 人员与初步工期

建议团队：

- 产品/项目经理 1 人。
- 后端工程师 2 人。
- 实时音频/Agent 工程师 1 人。
- Web/小程序前端工程师 1 人。
- 测试工程师 1 人。
- 运维/SRE 0.5～1 人。
- 运营商商务、微信账号运营、法务按阶段参与。

在运营商 DID 和微信账号资质及时到位的前提下，MVP 预计 8～10 周，小流量商用版本预计 10～12 周。最大外部依赖是线路审批、真实字段能力和微信消息资质，必须在 Sprint 0 并行推进。

## 14. 最终交付物

- 运营商线路与 SIP 字段验收报告。
- 用户开通、DID 分配和呼叫转移引导。
- 生产级呼入准入和多租户隔离。
- 完整的逐轮转写、录音、状态和通话报告。
- 规则标签、LLM 标签及人工修正后台。
- 微信身份绑定、消息通知和安全详情页。
- Outbox Worker、失败重试和人工补偿工具。
- 自动化测试、容量报告、监控告警和运维手册。
- 隐私、录音告知、数据保留和删除机制。
- 灰度方案、回滚方案及上线验收记录。

## 15. 上线总验收清单

- [ ] 真实手机三种呼叫转移场景通过。
- [ ] 专属 DID 能唯一、稳定地映射到用户。
- [ ] 主叫号码、时间、时长和挂断状态准确。
- [ ] 双方逐字稿完整，转写失败有明确标识。
- [ ] 报告可在 60 秒内生成，失败可恢复。
- [ ] 标签符合规则，用户可修改并留下审计。
- [ ] 微信绑定、通知、退订和补发均通过。
- [ ] 完整号码、逐字稿和录音不存在越权访问。
- [ ] 重复 SIP/Webhook/Outbox 事件不产生重复结果。
- [ ] 录音告知、数据保留、删除和同意证据通过法务验收。
- [ ] 峰值 1.5 倍容量测试及主要故障演练通过。
- [ ] 用户级、租户级和全局停止接听开关有效。

